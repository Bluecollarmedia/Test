name: Cloudflare Pages (auto-deploys via GitHub integration)

on:
  push:
    branches:
      - main
      - 'claude/**'

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cloudflare Pages deploys automatically on push"
